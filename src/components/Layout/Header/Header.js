import Navigation from "./Navigation";
import Hero from "./Hero";

const Header = (props) => {
  return (
    <header>
      <Navigation />
      <Hero />
    </header>
  );
};

export default Header;
